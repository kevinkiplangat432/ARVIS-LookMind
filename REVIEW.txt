// written to be reviewed 
internal/config/config.go
cmd/arvis/commands/server.go
internal/proxy/forwarder.go


// not yet to be reviewed
internal/store/requests.go and internal/store/anomalies.go
internal/detector/rules.go
internal/api/router.go and internal/api/handlers/
dashboard/src/App.tsx
dashboard/src/api/client.ts







questions to google once the internet is restored

what is the difference btw os.Stderr and os.Stdout and any other os.   and why is os.stderr used in NewTextHandler and os.Stdout use in NewJSONHandler

does using a slog.setDefault(logger) make all logging callour custom handler


read about this 
var (
	Stdin  = NewFile(uintptr(syscall.Stdin), "/dev/stdin")
	Stdout = NewFile(uintptr(syscall.Stdout), "/dev/stdout")
	Stderr = NewFile(uintptr(syscall.Stderr), "/dev/stderr")
)

